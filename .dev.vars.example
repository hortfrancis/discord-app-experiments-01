# Copy to `.dev.vars` (git-ignored) for `wrangler dev`.
# Find these in the Discord Developer Portal → your app → OAuth2.
DISCORD_CLIENT_ID=your_application_id
DISCORD_CLIENT_SECRET=your_client_secret
